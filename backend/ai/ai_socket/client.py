# this is was a test

# import socket

# HOST = '192.168.80.13'
# PORT = 2828

# client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# client.connect((HOST,PORT))


# client.send("tell me a fun fact about the 3 law of newton".encode("utf-8"))

# print(client.recv(1024).decode("utf-8"))
